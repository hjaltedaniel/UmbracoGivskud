<header class="site-header">
    <div class="container">
        <h1>@Model.Content.Title</h1>
    </div>
</header>

@*if
{
    <div class="flex-row">
        <div class="back">
            <a href="/index.html"><img src="assets/icons/nav/angle-left.svg" alt="Back"></a>
        </div>
        <h1>@Model.Content.Title</h1>
    </div>
}
else
{
    <h1>@Model.Content.Title</h1>
}*@
